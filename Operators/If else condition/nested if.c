#include<stdio.h>
void main(){
    int unit, id;
    char name;

    printf("Enter your id please: ");
    scanf("%d",&id);
    printf("Enter your unit please: ");
    scanf("%d",&unit);
    printf("Enter your name please: ");
    scanf("%c",&name);

    if(unit > 199){
        float charge = (199*1.20);
        if(charge > 400){
            total = (charge + charge * 0.15);
            printf("")
        }
    }
}
