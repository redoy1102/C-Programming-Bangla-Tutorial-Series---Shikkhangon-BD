#include<stdio.h>
int main(){
    int a, b, x, y, x + y,;
    printf("Enter the summation of two numbers: ");
    scanf("%d",&a);

    printf("Enter the sum of their square: ");
    scanf("%d",&b);

    x + y = a

    return 0;

}
